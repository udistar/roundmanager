[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Disable secrets scanning because it incorrectly flags public client-side API keys
  SECRETS_SCAN_ENABLED = "false"
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
